<Window x:Class="GreenChainz.Revit.UI.LeedResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LEED Certification Analysis - GreenChainz" Height="700" Width="850"
        MinHeight="550" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA" FontFamily="Segoe UI">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/GreenChainz.Revit;component/Themes/GreenChainzTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D6A4F" Padding="20,14">
            <StackPanel>
                <TextBlock Text="LEED Certification Analysis" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                <TextBlock x:Name="ProjectNameText" Text="Project Name" FontSize="11" Foreground="#B7E4C7" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Score Badges -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" CornerRadius="6" Padding="16" Margin="0,0,8,0" x:Name="CertBadge" Background="#2D6A4F">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Certification Level" FontSize="11" Foreground="#B7E4C7" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="CertLevelText" Text="GOLD" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" CornerRadius="6" Background="#40916C" Padding="16" Margin="0,0,8,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Points Earned" FontSize="11" Foreground="#B7E4C7" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="PointsText" Text="0/0" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" CornerRadius="6" Background="#D8F3DC" Padding="16">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Score" FontSize="11" Foreground="#6C757D" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="PercentText" Text="0%" FontSize="28" FontWeight="Bold" Foreground="#2D6A4F" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Credits DataGrid -->
            <DockPanel Grid.Row="1">
                <TextBlock Text="Credit Breakdown" DockPanel.Dock="Top" Style="{StaticResource GcSectionHeader}"/>
                <DataGrid x:Name="CreditsDataGrid" AutoGenerateColumns="False" IsReadOnly="True"
                          CanUserAddRows="False" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#DEE2E6"
                          RowBackground="White" AlternatingRowBackground="#F0F7F4"
                          BorderBrush="#DEE2E6" BorderThickness="1" FontSize="12" HeadersVisibility="Column">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#2D6A4F"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="BorderBrush" Value="#1B4332"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                        <DataGridTextColumn Header="Credit" Binding="{Binding CreditName}" Width="*"/>
                        <DataGridTextColumn Header="Points" Binding="{Binding PointsDisplay}" Width="70">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Achieved">
                                            <Setter Property="Foreground" Value="#2D6A4F"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Not Met">
                                            <Setter Property="Foreground" Value="#D62828"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Partial">
                                            <Setter Property="Foreground" Value="#FF9800"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Details" Binding="{Binding Description}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Export PDF" Click="ExportPdf_Click" Style="{StaticResource GcSecondaryButton}" Margin="0,0,8,0"/>
                <Button Content="Improve Score" Click="ImproveScore_Click" Style="{StaticResource GcPrimaryButton}" Margin="0,0,8,0"/>
                <Button Content="Close" Click="Close_Click" Style="{StaticResource GcSecondaryButton}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
