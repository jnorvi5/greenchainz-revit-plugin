<Window x:Class="GreenChainz.Revit.UI.LeedResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LEED Certification Analysis" Height="650" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Certification Level -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="LEED Certification Analysis" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock x:Name="ProjectNameText" Text="Project Name" FontSize="14" HorizontalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <!-- Score Display -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Certification Level Badge -->
            <Border Grid.Column="0" CornerRadius="10" Padding="20" Margin="5" x:Name="CertBadge" Background="#4CAF50">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Certification Level" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="CertLevelText" Text="GOLD" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Points Earned -->
            <Border Grid.Column="1" CornerRadius="10" Background="#2196F3" Padding="20" Margin="5">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Points Earned" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="PointsText" Text="0/0" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Percentage -->
            <Border Grid.Column="2" CornerRadius="10" Background="#FF9800" Padding="20" Margin="5">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Score" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="PercentText" Text="0%" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Credits DataGrid -->
        <DockPanel Grid.Row="2">
            <TextBlock Text="Credit Breakdown" DockPanel.Dock="Top" Style="{StaticResource HeaderStyle}"/>
            <DataGrid x:Name="CreditsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                      CanUserAddRows="False" GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#F5F5F5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                    <DataGridTextColumn Header="Credit" Binding="{Binding CreditName}" Width="*"/>
                    <DataGridTextColumn Header="Points" Binding="{Binding PointsDisplay}" Width="70">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="Achieved">
                                        <Setter Property="Foreground" Value="Green"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="Not Met">
                                        <Setter Property="Foreground" Value="Red"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="Partial">
                                        <Setter Property="Foreground" Value="Orange"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Details" Binding="{Binding Description}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Export PDF" Click="ExportPdf_Click" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Improve Score" Click="ImproveScore_Click" Padding="15,8" Margin="0,0,10,0" Background="#4CAF50" Foreground="White"/>
            <Button Content="Close" Click="Close_Click" Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>
