<Window x:Class="GreenChainz.Revit.UI.AuditResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GreenChainz.Revit.UI"
        mc:Ignorable="d"
        Title="Carbon Audit Results" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:ScoreToBrushConverter x:Key="ScoreToBrushConverter"/>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header and Overall Score -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="Overall Carbon Score:" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Border CornerRadius="50" Width="80" Height="80" Background="{Binding OverallScore, Converter={StaticResource ScoreToBrushConverter}}">
                <TextBlock Text="{Binding OverallScore}"
                           FontSize="32" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </StackPanel>

        <!-- Summary -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Audit Summary" Style="{StaticResource HeaderStyle}"/>
            <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" FontSize="14"/>
        </StackPanel>

        <!-- Material Breakdown -->
        <DockPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Material Breakdown" DockPanel.Dock="Top" Style="{StaticResource HeaderStyle}"/>
            <DataGrid ItemsSource="{Binding Materials}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Material Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"/>
                    <DataGridTextColumn Header="Carbon Impact" Binding="{Binding Carbon}" Width="120"/>
                    <DataGridTemplateColumn Header="Score" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Score, Converter={StaticResource ScoreToBrushConverter}}" CornerRadius="4" Padding="5,2">
                                    <TextBlock Text="{Binding Score}" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Action" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Swap Material" Click="SwapMaterial_Click" Margin="2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Recommendations -->
        <DockPanel Grid.Row="3" Margin="0,0,0,20" Height="100">
            <TextBlock Text="Recommendations" DockPanel.Dock="Top" Style="{StaticResource HeaderStyle}"/>
            <ListView ItemsSource="{Binding Recommendations}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="• " FontWeight="Bold"/>
                            <TextBlock Text="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Send RFQ for Flagged Materials" Click="SendRFQ_Click" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Export PDF" Click="ExportPDF_Click" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Close" Click="Close_Click" Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>
