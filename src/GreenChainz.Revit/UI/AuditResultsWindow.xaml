<Window x:Class="GreenChainz.Revit.UI.AuditResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Carbon Audit Results" Height="450" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Carbon Audit Report" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Carbon Score: " FontWeight="Bold" FontSize="16"/>
            <TextBlock x:Name="ScoreText" Text="Loading..." FontSize="16" Margin="5,0,20,0"/>

            <TextBlock Text="Rating: " FontWeight="Bold" FontSize="16"/>
            <TextBlock x:Name="RatingText" Text="-" FontSize="16" Foreground="Green"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Recommendations">
            <ListView x:Name="RecommendationsList">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Recommendation" Width="500" DisplayMemberBinding="{Binding}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <Button Grid.Row="3" Content="Close" Width="100" Height="30" Margin="0,10,0,0" HorizontalAlignment="Right" Click="CloseButton_Click"/>
    </Grid>
</Window>
