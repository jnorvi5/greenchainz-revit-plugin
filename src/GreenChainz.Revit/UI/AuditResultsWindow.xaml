<Window x:Class="GreenChainz.Revit.UI.AuditResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GreenChainz.Revit.UI"
        mc:Ignorable="d"
        Title="Carbon Audit Results" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:ScoreToBrushConverter x:Key="ScoreToBrushConverter"/>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header and Overall Score -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="Total Carbon:" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="ScoreText" Text="Loading..." FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Summary -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Audit Summary" Style="{StaticResource HeaderStyle}"/>
            <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" FontSize="14"/>
        </StackPanel>

        <!-- Material Breakdown -->
        <DockPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Material Breakdown" DockPanel.Dock="Top" Style="{StaticResource HeaderStyle}"/>
            <DataGrid x:Name="MaterialsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Material Name" Binding="{Binding MaterialName}" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"/>
                    <DataGridTextColumn Header="Carbon Factor" Binding="{Binding CarbonFactor, StringFormat=N2}" Width="120"/>
                    <DataGridTextColumn Header="Total Carbon (kgCO2e)" Binding="{Binding TotalCarbon, StringFormat=N0}" Width="150"/>
                    <DataGridTemplateColumn Header="Action" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Swap Material" Click="SwapMaterial_Click" Margin="2" Padding="5,2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Recommendations -->
        <DockPanel Grid.Row="3" Margin="0,0,0,20" MaxHeight="150">
            <TextBlock Text="Recommendations" DockPanel.Dock="Top" Style="{StaticResource HeaderStyle}"/>
            <ListView x:Name="RecommendationsList">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="• " FontWeight="Bold" Foreground="Green"/>
                            <TextBlock Text="{Binding Description}" Margin="0,0,10,0"/>
                            <TextBlock Text="Potential Savings: " FontStyle="Italic"/>
                            <TextBlock Text="{Binding PotentialSavings, StringFormat='{}{0:N0} kgCO2e'}" FontWeight="Bold" Foreground="Green"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Send RFQ" Click="SendRFQ_Click" Padding="15,8" Margin="0,0,10,0" Background="#007ACC" Foreground="White"/>
            <Button Content="Export PDF" Click="ExportPDF_Click" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Close" Click="Close_Click" Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>
