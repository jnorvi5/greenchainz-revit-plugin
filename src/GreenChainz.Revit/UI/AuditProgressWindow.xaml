<Window x:Class="GreenChainz.Revit.UI.AuditProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Carbon Audit - GreenChainz" Height="170" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="ToolWindow"
        Background="#F8F9FA" FontFamily="Segoe UI">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/GreenChainz.Revit;component/Themes/GreenChainzTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D6A4F" Padding="16,10">
            <TextBlock Text="Performing Carbon Audit..." FontSize="14" FontWeight="SemiBold" Foreground="White"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="24,20" VerticalAlignment="Center">
            <ProgressBar IsIndeterminate="True" Height="6" Foreground="#2D6A4F" Background="#DEE2E6"/>
            <TextBlock Text="Scanning model elements and calculating carbon impact..."
                       FontSize="12" Foreground="#6C757D" Margin="0,12,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
