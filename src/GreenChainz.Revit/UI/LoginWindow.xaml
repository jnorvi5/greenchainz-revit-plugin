<Window x:Class="GreenChainz.Revit.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GreenChainz Login" Height="340" Width="420"
        MinHeight="300" MinWidth="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F8F9FA" FontFamily="Segoe UI">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/GreenChainz.Revit;component/Themes/GreenChainzTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D6A4F" Padding="20,14">
            <TextBlock Text="GreenChainz Login" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="24,20,24,16">
            <TextBlock Text="Email" FontSize="11" Foreground="#6C757D" Margin="0,0,0,4"/>
            <TextBox x:Name="EmailTextBox" Style="{StaticResource GcTextBox}" Margin="0,0,0,12"/>

            <TextBlock Text="Password" FontSize="11" Foreground="#6C757D" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox" FontFamily="Segoe UI" FontSize="13" Padding="8,6"
                         BorderBrush="#DEE2E6" BorderThickness="1" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="LoginButton" Content="Login" Click="LoginButton_Click" Style="{StaticResource GcPrimaryButton}" Margin="0,0,8,0"/>
                <Button Content="Cancel" Click="CancelButton_Click" Style="{StaticResource GcSecondaryButton}"/>
            </StackPanel>

            <TextBlock x:Name="StatusText" FontSize="11" Foreground="#D62828" Margin="0,12,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
